(function() {
    var mod = angular.module('cases', ['cases.create']);

    mod.controller('CasesController', CasesController);

    function CasesController($scope, $http, $modal) {

    
    }
})();
