<!DOCTYPE html>
<html>
<head>
<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
<meta http-equiv="cache-control" content="max-age=0" />
<meta http-equiv="cache-control" content="no-cache" />
<meta http-equiv="expires" content="0" />
<meta http-equiv="expires" content="Tue, 01 Jan 1980 1:00:00 GMT" />
<meta http-equiv="pragma" content="no-cache" />
<title>Uforia ElasticSearch Admin panel</title>
<link href="css/admin.css" rel="stylesheet" type="text/css">
<link href="css/ui-lightness/jquery-ui-1.10.4.css" rel="stylesheet">
<script src="scripts/jquery-2.1.0.min.js" type="text/javascript"></script>
<script src="scripts/jquery-ui-1.10.4.min.js" type="text/javascript"></script>
<script src="scripts/spin.min.js" type="text/javascript"></script>
<script src="scripts/admin.js" type="text/javascript"></script>
</head>
<script>
(function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
(i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
})(window,document,'script','//www.google-analytics.com/analytics.js','ga');
ga('create', 'UA-52274211-1', 'auto');
ga('send', 'pageview');
</script>
<script>

var spinner, target;

//Loader settings 
var opts = {
  lines: 9, // The number of lines to draw
  length: 20, // The length of each line
  width: 10, // The line thickness
  radius: 30, // The radius of the inner circle
  corners: 1, // Corner roundness (0..1)
  rotate: 0, // The rotation offset
  direction: 1, // 1: clockwise, -1: counterclockwise
  color: '#000', // #rgb or #rrggbb or array of colors
  speed: 1, // Rounds per second
  trail: 60, // Afterglow percentage
  shadow: false, // Whether to render a shadow
  hwaccel: true, // Whether to use hardware acceleration
  className: 'spinner', // The CSS class to assign to the spinner
  zIndex: 2e9, // The z-index (defaults to 2000000000)
  top: '50%', // Top position relative to parent
  left: '50%' // Left position relative to parent
};

//init the page
$( document ).ready(function() {
  init();
  // target = document.getElementById('d3_visualization');
  // spinner = new Spinner(opts);
});


</script>
<body>
<div class="header">
  <p style="font-size: 18pt;"><b>U</b>niversal <b>For</b>ensic <b>I</b>ndexer and <b>A</b>nalyzer - Admin panel</p>
  <p style="font-size: 10pt;"><ul><li><b>Disclaimer 1:</b> sorry, we can't guarantee this website always works (correctly)! Please <a href="mailto:uforia@dhcp.net?Subject=Uforia Bug Report">report bugs</a> and/or <a href="https://github.com/uforia">submit a pull request</a>!</li>
  <li><b>Disclaimer 2:</b> we kindly request you to enable external Javascript, so we can log our visitors with Google Analytics - thank you in advance!</li>
</ul></p>
</div>
<div class="content">
  <div class="sidebarLeft">
    <div class="evidenceTypeBox">
      <h2>Available modules</h2>
      <select id="modules" multiple="multiple" class="listbox" onchange="changeModule();">
      </select>
    </div>
  </div>
  <div class="container">
    <h2>Select a module (Select multiple modules with control/command click or shift click). Then decide which field you want to have in a mapping and enter a name for the mapping.</h2>
    <input id="mapping_name" type="text" placeholder="Mapping name"/>
    <div id="content">
      <h2>Module fields</h2>
      <ul id="available_fields" class="connectedSortable">
      </ul>
      <h2>Mapping fields</h2>
      <ul id="selected_fields" class="connectedSortable">
      </ul>
      <h2>Visualization</h2>
      <ul id="visual_type" class="connectedSortable">
      </ul>
      <button id="create_mapping">Create mapping</button>
    </div>
  </div>
</div>
</body>
</html>
